//ให้ Comment ต่อท้ายถ้าหากว่าเป็น Expression แต่ถ้าหากไม่เป็นให้ปล่อยว่างเอาไว้ตามตัวอย่างนี้
50 + 12; // Expression

//Exercise #4: What is Expression Start coding here
130 + 300; // Expression
let zero = 1000 - 1000; // let ไม่เป็นแต่ 1000-1000 เป็น Expression
20 * 120; // Expression
1000 / 2; // Expression
typeof true; // Expression

// บรรทัด 10 นี้ John เป็น Expression เด้อ
// เพราะ name เป็น String ไปอ่าค่าใน varible เจอ John อยู่ข้างใน จึงเป็น Expression

let name = "John"; 

3 > 5; // Expression
10 == 100; // Expression
200; // Expression 
